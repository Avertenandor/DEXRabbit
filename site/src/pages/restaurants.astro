---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Партнёрство с ресторанами  DEXRabbit"
           description=" Поставки крольчатины в рестораны: качественное мясо, партнёрские условия, регулярные поставки от фермы в Подольске">
    
    <section class="section section-primary">
        <div class="container">
            <div class="section-header section-header-white">
                <h1 class="section-title"> Партнёрство с ресторанами</h1>
                <p class="section-description">
                    Качественная крольчатина для ресторанного бизнеса
                </p>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="container">
            <div class="restaurant-benefits">
                <h2> Преимущества сотрудничества</h2>
                <div class="benefits-grid">
                    <div class="benefit-card">
                        <h3> Премиум качество</h3>
                        <p>Мясо от кроликов с документированной родословной</p>
                    </div>
                    <div class="benefit-card">
                        <h3> Прозрачность</h3>
                        <p>Полная отчётность по условиям содержания и кормления</p>
                    </div>
                    <div class="benefit-card">
                        <h3> Стабильность поставок</h3>
                        <p>Регулярные поставки по согласованному графику</p>
                    </div>
                    <div class="benefit-card">
                        <h3> Выгодные цены</h3>
                        <p>Прямые поставки от производителя без посредников</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="section section-gray">
        <div class="container">
            <div class="partnership-form-section">
                <h2> Заявка на партнёрство</h2>
                <form id="partnership-form" class="contact-form">
                    <div class="form-group">
                        <label for="restaurant-name">Название ресторана *</label>
                        <input type="text" id="restaurant-name" name="restaurant-name" required>
                    </div>
                    
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="contact-person">Контактное лицо *</label>
                            <input type="text" id="contact-person" name="contact-person" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Телефон *</label>
                            <input type="tel" id="phone" name="phone" required>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="email">Email *</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="monthly-volume">Планируемый месячный объём (кг)</label>
                        <select id="monthly-volume" name="monthly-volume">
                            <option value="">Выберите объём</option>
                            <option value="10-50">10-50 кг</option>
                            <option value="50-100">50-100 кг</option>
                            <option value="100-200">100-200 кг</option>
                            <option value="200+">Более 200 кг</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="additional-info">Дополнительная информация</label>
                        <textarea id="additional-info" name="additional-info" rows="4"></textarea>
                    </div>
                    
                    <!-- Honeypot для защиты от спама -->
                    <input type="text" name="honeypot" style="display:none;">
                    
                    <button type="submit" class="btn btn-primary btn-large">
                         Отправить заявку
                    </button>
                </form>
            </div>
        </div>
    </section>

</BaseLayout>

<style>
.benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
}

.benefit-card {
    background: white;
    padding: 1.5rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.benefit-card h3 {
    margin-bottom: 0.75rem;
    color: var(--color-primary);
}

.partnership-form-section {
    max-width: 600px;
    margin: 0 auto;
}

.contact-form {
    background: white;
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.1);
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
}

.form-group input,
.form-group select,
.form-group textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 1rem;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.1);
}

@media (max-width: 768px) {
    .form-grid {
        grid-template-columns: 1fr;
    }
}
</style>

<script>
document.getElementById('partnership-form').addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Проверка honeypot
    if (this.honeypot.value) return;
    
    const formData = new FormData(this);
    const data = Object.fromEntries(formData);
    
    // Аналитика
    if (window.analytics && typeof window.analytics.trackEvent === 'function') {
        window.analytics.trackEvent('restaurant_partnership_form_submit', {
            restaurant: data['restaurant-name'],
            volume: data['monthly-volume']
        });
    }
    
    // Пока заглушка - в реальности отправка на сервер
    if (window.showToast) {
        window.showToast(' Заявка отправлена! Мы свяжемся с вами в течение 24 часов.', 'success');
    } else {
        alert(' Заявка отправлена! Мы свяжемся с вами в течение 24 часов.');
    }
    
    this.reset();
});
</script>
